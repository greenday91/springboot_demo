<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.web.dao.ArticleDao">

	<select id="getById" resultType="com.example.demo.web.entity.Article" parameterType="java.lang.String">
		select * from article where id = #{id}
	</select>

	<insert id="insertArticle">
		insert into article
		(
		  id,
		  title,
     	  content,
     	  autr,
    	  createdate,
          createby,
          updatetime,
          updateby
		)
		values
		(
		#{id},
		#{title},
		#{content},
		#{autr},
		#{createDate},
		#{createBy},
		#{updateTime},
		#{updateBy}
		)
	</insert>



</mapper>