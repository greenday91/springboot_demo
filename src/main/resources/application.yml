spring:
  profiles:
    active: dev

server:
  port: 8089

mybatis:
  mapper-locations: classpath*:mappings/*.xml
  type-aliases-package: com.chenzy.demo.web.pojo


logging:
  level:
    com.example.demo.web.demo: debug
    com.example.demo.config: debug
    org:
      springframework:
        boot:
          autoconfigure: ERROR
  file: article.log